{
  "name": "re-relating",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html src/*.html --https",
    "build": "rm -rf public/* && parcel build src/index.html src/*.html --dist-dir public --no-content-hash"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "cssnano": "^5.0.13",
    "parcel": "^2.0.1",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "postcss": "^8.4.5"
  },
  "staticFiles": {
    "staticPath": [
      {
        "staticPath": "src/assets",
        "staticOutDir": "assets"
      },
      {
        "staticPath": "src/test-image-model",
        "staticOutDir": "test-image-model"
      }
    ]
  }
}

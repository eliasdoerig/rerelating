{
  "name": "re-relating",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html src/*.html --https",
    "build": "rm -rf public/ && parcel build src/index.html src/*.html --out-dir public/ --no-minify --no-content-hash"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^5.0.13",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "postcss": "^8.4.5",
    "sass": "^1.45.0"
  },
  "staticFiles": {
    "staticPath": [
      {
        "staticPath": "src/assets",
        "staticOutDir": "assets"
      },
      {
        "staticPath": "src/test-image-model",
        "staticOutDir": "test-image-model"
      }
    ]
  }
}
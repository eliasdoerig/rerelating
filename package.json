{
  "name": "re-relating",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel src/index.html src/*.html --https",
    "build": "rm -rf public/* && parcel build src/index.html src/*.html --dist-dir public --no-content-hash"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "cssnano": "^5.0.13",
    "parcel": "^2.0.1",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "postcss": "^8.4.5"
  },
  "staticFiles": {
    "staticPath": "src/test-image-model",
    "distDir": "public/test-image-model"
  }
}